name: CI Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Baixar o código
        uses: actions/checkout@v4

      - name: Build da imagem Docker (pedidos)
        run: docker build -t pedidos-veloz/pedidos:ci ./pedidos
